name: yangji
description: YANGJI - 태그 기반 뉴스 인사이트 플랫폼

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  http: ^1.1.0
  shared_preferences: ^2.2.2
  cached_network_image: ^3.3.0
  shimmer: ^3.0.0
  intl: ^0.18.1
  url_launcher: ^6.2.2
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0
  provider: ^6.1.1
  timeago: ^3.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
